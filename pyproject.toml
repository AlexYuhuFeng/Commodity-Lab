[project]
name = "commodity-lab"
version = "1.0.0"
description = "Private — Commodity Lab (Internal use only). Not open source."
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["core"]

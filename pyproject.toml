[project]
name = "commodity-lab"
version = "1.0.12"
description = "Private Commodity Lab workspace for virtual commodity hedging and AI-assisted analysis."
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["core"]

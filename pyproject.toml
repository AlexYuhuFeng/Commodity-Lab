[project]
name = "commodity-lab"
version = "0.1.0"
description = "A comprehensive data analytics platform for commodity trading and analysis"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["core"]

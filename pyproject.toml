[project]
name = "commodity-lab"
version = "1.5.0"
description = "AI-guided commodity hedging training terminal."
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["core"]
